{
  "name": "obsidian-docx-claude",
  "version": "0.1.0",
  "description": "Read-only .docx preview for Obsidian with zoom.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["obsidian", "docx", "preview"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "docx-preview": "^0.3.0"
  }
}
